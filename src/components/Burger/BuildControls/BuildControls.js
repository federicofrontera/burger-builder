import React from 'react';
import styles from './BuildControls.module.css'

const buildControls = () => {
    return (
        <div>
            
        </div>
    );
};

export default buildControls;
